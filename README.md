```bash
npm run dev

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.

#After reboot:
1. Docker Desktop auto-starts (no action needed)
2. cd ~/sailbook && supabase start
3. npm run dev

#run all tests
supabase db reset && npx playwright test