---
session: 135
dev: eric
slug: staging
branch: staging
started: 2026-05-13T23:22:46Z
ended: 2026-05-14T02:01:43Z
wall_clock: 2.67
dev_time: 2.08
review_time: 0.58
duration: 2.08
points: 5
status: closed
pr_number: 52
pr_url: https://github.com/mobiustripper42/sailbook/pull/52
pr_opened_at: 2026-05-14T01:27:40Z
transcript: /home/eric/.claude/projects/-home-eric-sailbook/badc3fc1-6220-4fa6-970e-93e0bf70a09b.jsonl
---

# Session 135 — staging

**Task:** Phase 9 launch-blocker cleanup — close out the doable §F + §J items, defer SMS

**Completed:**
- F.2 anon registration end-to-end on staging — confirmation email + redirect to original course page both work (required syncing custom SMTP on staging Supabase first)
- F.5 cancel + restore enrollment flow on staging — full path verified; flagged restore-without-rebill gap (accepted for V2)
- F.7 cron live-fire — verified passively via Vercel cron run history, skipped the temp-CRON_SECRET juggle
- J.3 prod Supabase Pro confirmed on Pro plan; email rate limit 30/hr verified sufficient
- J.5 (covers J.4) — staging Supabase auth-panel config brought to prod parity: custom SMTP toggle, email templates, password policy, Site URL / redirect URLs, Google OAuth. Staging Google OAuth now works.
- SMS_ENABLED kill-switch — gates SMS at the channel-enabled predicate in `src/lib/notifications/preferences.ts`; UI hides SMS toggles + A2P consent text when off. CI sets `SMS_ENABLED=true` so test coverage stays. 28 affected Playwright specs pass.
- PROJECT_PLAN.md cleanup pass — closed A.4, A.5, D.1, D.2, F.6, G.1, G.3, H.2, H.3, H.4, I.1–I.4, J.1, J.2, J.6, K (operational + post-launch checklist items either ad-hoc, accepted-risk, or no-launch-gate). Marked F.2/F.5/F.7/J.3/J.4 done; added J.5 + Resend Pro lines.
- Cleaned up 1 orphan remote branch (`claude/setup-sailbook-staging-eV1Eu`) + pruned ~35 stale local refs.

**In Progress:** Nothing.

**Blocked:**
- Resend Pro upgrade (task #6 in this session's queue) — needs your card; required before public launch volume.
- Andy-dependent items still open: §C Stripe live keys, F.3/F.4 Stripe e2e + refund, F.10 mobile pass, H.1 Andy briefing.
- G.2 Supabase plan-limit alerts left `[~]` deferred even though prod is on Pro now — doable any time, just wasn't pulled in this session.

**Next Steps:**
1. On Vercel Prod + Preview: confirm `SMS_ENABLED` is unset (or `false`). Optionally clear `TWILIO_*` env vars as belt-and-suspenders.
2. Upgrade Resend to Pro ($20/mo, 50k/mo) before announcing launch.
3. Tap PR #52 preview URL → smoke test the SMS-off UI on student account + register + admin prefs at 375px.
4. Merge PR #52 when Andy's QA pass on staging is clear; this is the next batch into staging.

**Context:**
- The "email rate limit 2/hr" mystery on staging was unconfigured custom SMTP, not Resend volume. Built-in Supabase SMTP caps hard at 2/hr regardless of plan — flipping the custom SMTP toggle ON is what unlocks the configurable rate-limit field.
- Staging Supabase dashboard config does NOT sync from migrations or config.toml. Every auth-panel item (SMTP, templates, OAuth, Site URL, rate limits, password policy) is per-project and must be matched manually. Worth a docs note for any future "spin up another env" exercise.
- Restore-cancelled-enrollment intentionally doesn't re-bill — Andy handles that offline. Logged as a known gap.
- The `?? true` default in the SMS-disable save path will need an audit when A2P re-activates SMS: anyone with `student_global.sms = true` whose row was last touched while SMS was off didn't actively opt in. Matches the documented "default-on for unset" posture, not a regression — but flag for A2P compliance audit at re-enable time.
- `smsEnabled` prop defaults to `false` on every consuming client component — intentional fail-closed (forgetting to pass it hides SMS in prod, which is the safe direction).
- A2P 10DLC is now treated as post-V2. SMS_ENABLED env-var is the lever; flip both code paths and the consent text re-render automatically.

**Code Review:** Clean bill of health from @code-review. Two advisory notes captured in the PR body — A2P-flip audit + fail-closed default justification. No blocking findings. SMS-leak audit performed: every `sendSMS()` callsite sits inside a channel-enabled gate; no leaky path.
