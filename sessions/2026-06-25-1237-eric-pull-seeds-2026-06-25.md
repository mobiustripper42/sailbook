---
session: 137
dev: eric
slug: pull-seeds-2026-06-25
branch: task/pull-seeds-2026-06-25
started: 2026-06-25T12:37:15Z
ended:
points:
pr_numbers: [88, 90, 92, 93]
status: open
transcript: /home/eric/.claude/projects/-home-eric-sailbook/019d03cc-fb32-47b3-b6a1-566d7d42bcd0.jsonl
---

# Session 137 — pull-seeds-2026-06-25

<!-- Task blocks appended by /kill-this, one per task. -->

## Task 1: DEC-014 main-side cleanup + record Vercel sessions-branch deploy gate (DEC-030)

**Completed:**
- `.gitignore` — ignore `.sessions-worktree/` (was showing as `?? untracked`; could be added as a gitlink with `git add -A`). Closes the DEC-S014 main-side gap carried from Session 136.
- Removed `sessions/` from `main` (`2026-05-03-…ev1eu.md`, `2026-05-13-eric-staging.md`) — both already on the orphan `sessions` branch. `session-log-archive.md` untouched.
- `docs/DECISIONS.md` — **DEC-030**: Vercel dashboard *Ignored Build Step* `bash -c '[ -f package.json ] && exit 1 || exit 0'` gates the `sessions` branch from deploys. Documents why `vercel.json` `git.deploymentEnabled` doesn't work for an orphan branch (Vercel reads the pushed branch's config, which is empty). Same approach as `~/muster`.

**Outstanding (Eric's action):** set the Vercel Ignored Build Step in the dashboard — not version-controlled, can't be done from the repo.

**Code review:** Docs/config only — no application code. Self-reviewed, no agent run.
**PR:** [#88](https://github.com/mobiustripper42/sailbook/pull/88)
**Points:** 1
**Branch:** task/dec-014-cleanup
**Opened at:** 2026-06-25T17:32:53Z

## Task 2: Reconcile seed-date references in QA.md + dev-seed-qa.sql (#85)

**Completed:**
- `docs/dev-seed-qa.sql` — anchored all 10 literal session dates to `current_date ± N` offsets, mirroring #70's scheme in `seed.sql`/`demo-seed.sql`: weekend `+40/+41`, evening series `+5`(cancelled opener)/`+12/+19/+26`, dinghy draft `+10`, cancelled open-sailing `-20`, completed `-80/-79`. Header + quick-reference comments reconciled to offsets/roles. Verified no `2026-..` literals remain.
- `docs/QA.md` — rewrote ~30 manual-test-step references from literal dates/weekdays to **session role + `d00X` ID** (e.g. "the cancelled opener (d003)", "the two Weekend Intensive sessions"). Dropped weekday assertions ("Saturday"/"Wednesday") since only the 7-day cadence is fixed under `current_date` offsets — the weekday floats. Grep-confirmed clean except the two intentional `ASA 101 March` course-title labels (display-only, per seed policy).
- Fast-follow to #70; closes the last seed-date drift. No code, nothing CI-gated.

**Code review:** Docs only — no application code. Self-reviewed, no agent run.
**PR:** [#90](https://github.com/mobiustripper42/sailbook/pull/90)
**Points:** 2
**Branch:** task/85-qa-seed-dates
**Opened at:** 2026-06-25T18:12:45Z

## Task 3: Add Open Sailing as the first card on /sailing-classes (#78)

**Completed:**
- `src/app/sailing-classes/page.tsx` — prepended an Open Sailing card (first, ahead of ASA101) using Andy's copy + `3 Hours – $70`; `registerHref: /courses/open` (the seeded drop-in `course_types.slug`, **not** `/courses/open-sailing` as the issue guessed). Added an optional `imageClassName` to the card type; Open Sailing uses `object-cover object-left` so the 4:3 photo (`public/sailing-classes/open-sailing.jpg`, dropped on main by Andy) fills the square frame like the square ASA images — keeps skyline + crew, trims only open water. Existing cards default to `object-contain`, unchanged.
- `supabase/seed.sql` — bumped the five Open Sailing drop-in courses `$65 → $70` to match production (closes the marketing-vs-demo price split flagged earlier).
- `tests/sailing-classes.spec.ts` (new) — first-card order, `/courses/open` link, copy/price, image; runs across all viewport projects incl. 375px.
- Verified: lint clean, `npm run build` green (`/sailing-classes` static), 6/6 Playwright pass (desktop + mobile), 375px screenshot confirmed the `object-left` crop frames the photo correctly.

**Code review:** @code-review — Clean Bill of Health, no blocking issues. Slug, seed completeness, no-regression on square images, a11y, conventions all verified.
**PR:** [#92](https://github.com/mobiustripper42/sailbook/pull/92)
**Points:** 3
**Branch:** task/78-open-sailing-card
**Opened at:** 2026-06-29T22:02:34Z

## Task 4: Email one-time-code sign-in via Supabase OTP, flag-gated (#79)

**Completed:**
- Phase 1 of passwordless (DEC-031): email 6-digit-code sign-in alongside password + Google, dark behind `NEXT_PUBLIC_EMAIL_CODE_AUTH`, all roles. `signInWithOtp({ shouldCreateUser: false })` + `verifyOtp` — sign-in only for existing accounts; zero DB impact (no migration, no `handle_new_user`, no RLS).
- `src/lib/auth/role-redirect.ts` (new, `redirectForRole` extracted + reused in login/updatePassword/verifyEmailCode); `(auth)/actions.ts` (`requestEmailCode` + `verifyEmailCode`, both flag-hard-gated server-side); `email-code-form.tsx` (new two-step `useState` client component); `login/page.tsx` (flag-conditional render); `magic_link.html` + `config.toml` (repurpose unused Magic Link template → `{{ .Token }}`); `tests/email-code-auth.spec.ts` (new); DEC-031; `.env.example`.
- Designed with @architect (validated `shouldCreateUser:false`, DEC-024/invites/deliverability interactions, security model). Verified: lint clean, `npm run build` green, **2/2 Playwright pass** (happy via Mailpit + enumeration), 375px screenshot confirmed.
- **@code-review caught a real enumeration leak** (request action surfaced the per-user resend-throttle 429, which only fires for existing accounts). Fixed: request step surfaces only pre-send input-format errors, swallows all send-side results, `console.error`s the unexpected ones; DEC-031 notes the residual timing channel.

**Code review:** One security finding (enumeration via throttle error) — fixed before PR. Rest clean.
**PR:** [#93](https://github.com/mobiustripper42/sailbook/pull/93)
**Points:** 5
**Branch:** task/79-email-code-auth
**Opened at:** 2026-06-29T23:34:51Z

**Next Steps:**
- **PROD STRIPE BUG (live):** stale **test-mode** `stripe_customer_id` on profiles → "No such customer" on Pay (digest 3682477675). Fix: `UPDATE profiles SET stripe_customer_id = NULL WHERE stripe_customer_id IS NOT NULL;` in prod Supabase, then retry. Recommended follow-up: harden checkout to recreate on `resource_missing`.
- **#79 enablement** (config, gates flipping the flag): Magic Link template → `{{ .Token }}` in staging + prod dashboards; raise prod `email_sent` rate limit; confirm auth SMTP delivers in prod; set `NEXT_PUBLIC_EMAIL_CODE_AUTH` in Vercel.

**Context:**
- Local Supabase was restarted this session (to load the new Magic Link template); left running. Dev server stopped.

**Context:**
